# Gatitobook

This project was generated with [Angular CLI](https://github.com/angular/angular-cli) version 14.1.1.

## Development server

Run `ng serve` for a dev server. Navigate to `http://localhost:4200/`. The application will automatically reload if you change any of the source files.

## Tools used

--strict: for more type checking in the project;
bootstrap: configured as global cd css framework for application;
uses Angular module in the application with lazy loading technique, improving initial loading;
